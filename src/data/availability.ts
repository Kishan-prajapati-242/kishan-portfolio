// CONTENT.md section 9, "Availability, exactly this".
//
// One string, three placements: the hero badge uses the short form, the footer
// and /contact use the full one. Kept here rather than typed out in three
// components so there is no way for them to drift, which is what happened to
// the work-authorization line this replaces.
export const AVAILABILITY_FULL =
  'Open to software engineering internships and co-ops. Available from January 2027, with Summer 2027 as my main target. F-1 student, CPT and OPT eligible.';

export const AVAILABILITY_SHORT = 'Open to SWE internships, from January 2027';
