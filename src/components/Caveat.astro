---
// DESIGN.md section 6. This is not a warning box and must not look like one:
// no background fill, no icon, no rounded corners, no border except the rule
// on the left. --color-annot is only ever used for caveats and limitations.
---
<aside class="caveat">
  <p class="t-label tag">Caveat</p>
  <div class="body">
    <slot />
  </div>
</aside>

<style>
  .caveat {
    border-left: 2px solid var(--color-annot);
    padding-left: 16px;
    margin-block: 1.5rem;
  }

  .tag {
    color: var(--color-annot);
    margin: 0 0 0.25rem;
  }

  .body {
    color: var(--color-ink-2);
    font-size: var(--text-small);
  }

  .body :global(p) {
    margin-block: 0.5em;
  }

  .body :global(p:first-child) {
    margin-top: 0;
  }

  .body :global(p:last-child) {
    margin-bottom: 0;
  }
</style>
